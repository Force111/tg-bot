services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: telegram-bot:local
    container_name: telegram-bot
    env_file:
      - .env
    restart: unless-stopped
    init: true
